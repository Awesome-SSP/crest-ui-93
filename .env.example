# Example env for local dev or when sharing on LAN
# HOST: what interface Vite binds to (0.0.0.0 to be reachable from other machines)
HOST=0.0.0.0
# PORT: dev server port; if not set, Vite will fall back to configured default (8080)
PORT=8080

# When serving production build with `npm run serve:prod`, set PORT if needed
# PORT=8080
